package main

var (
	// GitBuild number injected at the build time
	GitBuild string = "undefined"
	// Version number injected at the build time
	Version string = "undefined"
	// BuildTime time of current build injected at the build time
	BuildTime string = "undefined"
)
